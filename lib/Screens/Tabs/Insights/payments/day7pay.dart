import 'package:flutter/material.dart';

class Day7Pay extends StatelessWidget {
  const Day7Pay({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}