import 'package:flutter/material.dart';

class MonthPay extends StatelessWidget {
  const MonthPay({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}